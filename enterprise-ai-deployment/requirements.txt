# ================================================================================
# requirements.txt  —  Python dependency manifest
# ================================================================================
#
# Pinned to minor versions for reproducible installs.
# Run:  pip install -r requirements.txt
#
# Optional (uncomment to enable):
#   mlflow     — experiment tracking for retrain runs
#   streamlit  — customer-facing dashboard
#   altair     — charts for the Streamlit dashboard
# ================================================================================

# ── Core ML & data ────────────────────────────────────────────────────────────
numpy>=1.26,<2.0
pandas>=2.1,<3.0
scikit-learn>=1.4,<2.0
scipy>=1.12,<2.0
joblib>=1.3,<2.0

# ── API server ────────────────────────────────────────────────────────────────
fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0

# ── HTTP client (dashboard → API calls) ──────────────────────────────────────
requests>=2.31,<3.0

# ── Dashboard (optional — comment out if not using Streamlit) ────────────────
streamlit>=1.33,<2.0
altair>=5.3,<6.0

# ── Experiment tracking (optional — comment out if MLflow server unavailable) ─
# mlflow>=2.12,<3.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.1,<9.0
pytest-cov>=5.0,<6.0
